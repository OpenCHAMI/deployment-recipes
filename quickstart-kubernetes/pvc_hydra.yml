kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  namespace: ochami
  name: jwt-configs
spec:
  storageClassName: ceph-cephfs-external
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
