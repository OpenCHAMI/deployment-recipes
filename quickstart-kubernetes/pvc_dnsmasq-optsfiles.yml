kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  namespace: ochami
  name: dnsmasq-optsfiles
spec:
  storageClassName: ceph-cephfs-external
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
